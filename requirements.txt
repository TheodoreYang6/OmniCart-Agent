# ============================================================
# OmniCart Agent 依赖 (V3 — Docker 部署版)
# 安装: pip install -r requirements.txt
# ============================================================

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# 数据校验 + 配置治理
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP 客户端
httpx>=0.28.0

# 环境变量
python-dotenv>=1.0.0

# 文件上传
python-multipart>=0.0.18

# Qwen API (OpenAI-compatible SDK)
openai>=1.0.0

# 本地模型 (embedding / reranker，OMNICART_USE_LOCAL_MODELS=true 时启用)
# 注：体积较大；纯 API / MOCK 部署可不装，Provider 懒加载不会在导入期触发 torch。
torch>=2.2.0
transformers>=4.51.0
sentence-transformers>=3.0.0

# 中文分词（BM25 稀疏向量编码，混合检索词面召回侧）
jieba>=0.42.1

# 向量数据库（需 >=1.14 以支持 sparse 向量 + Prefetch/FusionQuery 服务端 RRF）
qdrant-client>=1.14.0

# Agent 编排 (LangGraph)
langgraph>=0.2.0
langgraph-checkpoint>=2.0.0

# 缓存 (Redis)
redis>=5.0.0

# 结构化存储 (PostgreSQL)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
nest-asyncio>=1.6.0

# YAML 配置
pyyaml>=6.0

# 测试
pytest>=9.0.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
