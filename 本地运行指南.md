# OmniCart Agent 本地运行指南（macOS）

> 本文档描述在本机（非 Docker）完整跑起 **后端 + Web 前端 + 三个数据库** 的操作步骤。
> 所有命令均已在本机实测验证。数据库为 Homebrew / 本地二进制安装，**无需 Docker**。

---

## 0. 架构与端口一览

| 组件 | 地址 / 端口 | 说明 |
|---|---|---|
| Web 前端 (Vite) | http://localhost:5173 | React + TS，`web-client/` |
| 后端 (FastAPI) | http://127.0.0.1:8006 | `backend/app/main.py` |
| PostgreSQL 16 | 127.0.0.1:5432 | 库 `omnicart`，已建表并含 105 条商品 |
| Qdrant | 127.0.0.1:6333 (gRPC 6334) | 向量库，已含 `products` 集合 |
| Redis | 127.0.0.1:6379 | 缓存 |

- Python 环境：`/Users/yangqiduo/miniforge3/envs/omnicart/bin/python`
- Node/npm：已安装（`node -v` / `npm -v`）

---

## 1. 启动数据库

### 1.1 PostgreSQL（Homebrew，已开机自启）
- 二进制：`/opt/homebrew/bin/postgres`
- 数据目录：`/opt/homebrew/var/postgresql@16`
- 库 `omnicart` 连接账号：系统用户 `yangqiduo`（trust 认证，无密码）

```bash
brew services start postgresql@16     # 启动（通常已在运行）
brew services info  postgresql@16     # 查看状态
brew services stop  postgresql@16     # 停止
# 验证：
/opt/homebrew/bin/psql -d omnicart -tAc "select count(*) from products;"   # → 105
```

### 1.2 Redis（Homebrew，已开机自启）
- 二进制：`/opt/homebrew/bin/redis-server`
- 配置：`/opt/homebrew/etc/redis.conf`，数据：`/opt/homebrew/var/db/redis/`

```bash
brew services start redis
brew services stop  redis
redis-cli ping                         # → PONG
```

### 1.3 Qdrant（本地二进制，需手动启动）
- 二进制：`~/.local/bin/qdrant`
- **数据/配置目录：`~/.local/share/qdrant/`**（含 `storage/`、`config.yaml`）
- ⚠️ 必须在该目录内启动，才能复用已建好的 `products` 集合（Qdrant 默认用当前目录下的 `./storage`）。

```bash
# 后台启动
cd ~/.local/share/qdrant && nohup ~/.local/bin/qdrant > qdrant.run.log 2>&1 &

# 验证：
curl http://127.0.0.1:6333/collections    # → 应包含 {"name":"products"}

# 停止：
lsof -ti:6333 | xargs kill
```

---

## 2. 启动后端

后端从**项目根目录的 `.env`** 读取配置。仓库根已生成 `.env`（连接本机三库、`MOCK_MODE=true`、未填 `QWEN_API_KEY`）。

### ⚠️ 关键：必须加 `--loop asyncio`
PG 仓库用 `nest_asyncio` 做同步→异步桥接，而 uvicorn 默认的 `uvloop` **无法被 nest_asyncio 兼容**
（报错 `Can't patch loop of type uvloop.Loop`，导致所有 PostgreSQL 接口 500）。
因此数据库模式下启动**务必带 `--loop asyncio`**。

```bash
cd /Users/yangqiduo/amap-ai-agent/OmniCart-Agent

/Users/yangqiduo/miniforge3/envs/omnicart/bin/python -m uvicorn app.main:app \
  --app-dir backend \
  --host 127.0.0.1 --port 8006 \
  --loop asyncio
```
- 从**项目根目录**启动（CWD=根），保证 `.env` 被 pydantic-settings 读到。
- `--app-dir backend` 让 `app.main` 可被导入。
- 看到 `Application startup complete` 即成功。

> 说明：项目自带的 `python run.py` 一键脚本**未带 `--loop asyncio`**，在数据库模式下会导致 PG 接口 500；数据库模式请用上面的直连命令。（若只想跑 JSON 降级模式，删除 `.env` 后 `run.py` 可用。）

---

## 3. 启动前端

`web-client/.env.local` 已配置 `VITE_API_BASE=http://127.0.0.1:8006`，自动连本机后端。

```bash
cd /Users/yangqiduo/amap-ai-agent/OmniCart-Agent/web-client
npm install        # 首次或依赖更新时
npm run dev        # 开发模式 → http://localhost:5173
```

生产构建（可选）：
```bash
npm run build      # 产物在 web-client/dist/
npm run preview    # 本地预览构建产物
```

---

## 4. 完整启动顺序（从零到可用）

```bash
# ① 数据库
brew services start postgresql@16
brew services start redis
cd ~/.local/share/qdrant && nohup ~/.local/bin/qdrant > qdrant.run.log 2>&1 &

# ② 后端（新终端）
cd /Users/yangqiduo/amap-ai-agent/OmniCart-Agent
/Users/yangqiduo/miniforge3/envs/omnicart/bin/python -m uvicorn app.main:app \
  --app-dir backend --host 127.0.0.1 --port 8006 --loop asyncio

# ③ 前端（新终端）
cd /Users/yangqiduo/amap-ai-agent/OmniCart-Agent/web-client && npm run dev
```
浏览器打开 http://localhost:5173

---

## 5. 验证

```bash
# 三个库是否都连上（应返回 postgres/qdrant/redis 均为 connected）
curl -s http://127.0.0.1:8006/api/health

# 商品接口是否返回真实数据（total=105）
curl -s "http://127.0.0.1:8006/api/products?page_size=3"
```
期望输出：
```json
{"status":"ok","service":"omnicart-agent","version":"2.0.0",
 "postgres":"connected","qdrant":"connected","redis":"connected"}
```
前端逐页自查：商品页有图有价 / 详情页 / 加购 → 购物车 / 小O对话（SSE 流式）。

---

## 6. 停止服务

```bash
# 前端 / 后端（按端口）
lsof -ti:5173 | xargs kill -9      # 前端
lsof -ti:8006 | xargs kill -9      # 后端
lsof -ti:6333 | xargs kill -9      # Qdrant

# 数据库（brew 托管，一般无需停）
brew services stop postgresql@16
brew services stop redis
```

---

## 7. 配置说明与切换

### 7.1 根目录 `.env`（后端）
```bash
DATABASE_URL=postgresql+asyncpg://yangqiduo@127.0.0.1:5432/omnicart   # PG，系统用户，无密码
QDRANT_URL=http://127.0.0.1:6333                                       # Qdrant
REDIS_URL=redis://127.0.0.1:6379/0                                     # Redis
OMNICART_MOCK_MODE=true                                                # 见 7.3
# QWEN_API_KEY=                                                        # 见 7.3
```
- `use_postgres / use_qdrant / use_redis` 由对应连接串是否为空**自动推导**；填了就启用，删空/删 `.env` 就降级到 JSON + 内存模式。

### 7.2 前端指向哪个后端
- `web-client/.env.local` → `VITE_API_BASE=http://127.0.0.1:8006`（本地）。
- 删除该文件即回退到默认线上后端 `http://8.137.187.54:8006`（见 `vite.config.ts`）。

### 7.3 MOCK 模式 vs 真实大模型
- 当前 `OMNICART_MOCK_MODE=true` 且未配 `QWEN_API_KEY`：商品/购物车/检索/推荐链路可跑，但**自然语言理解与图片视觉识别用的是模板/降级结果**。
- 需要真实 LLM/视觉：在 `.env` 填 `QWEN_API_KEY=xxx` 并把 `OMNICART_MOCK_MODE=false`，重启后端。

---

## 8. 常见问题（踩坑记录）

| 现象 | 原因 | 解决 |
|---|---|---|
| `/api/products` 报 500，日志 `Can't patch loop of type uvloop.Loop` | uvicorn 默认 uvloop 与 nest_asyncio 不兼容 | 启动加 `--loop asyncio` |
| 商品加载失败 / 一直转圈 | 没有 `.env`，后端降级 JSON 且前端连的后端没起 | 建 `.env` + 按本文启动后端 |
| Qdrant 起来了但没有 `products` 集合 | 不在数据目录启动，用了空的 `./storage` | 必须 `cd ~/.local/share/qdrant` 再启动 |
| 后端连不上 PG / 认证失败 | 用了 `omnicart:omnicart` 账号（本机不存在该角色） | 用系统用户 `yangqiduo`，trust 无密码 |
| 前端拿不到数据但后端正常 | `.env.local` 指向了线上或被删 | 确认 `VITE_API_BASE=http://127.0.0.1:8006` |

---

## 9. 数据初始化（仅在需要重建时）

正常情况下 PG 与 Qdrant 数据已就绪，**无需重跑**。如需重建：
```bash
# PG 表结构（Alembic 迁移）
cd /Users/yangqiduo/amap-ai-agent/OmniCart-Agent
/Users/yangqiduo/miniforge3/envs/omnicart/bin/alembic upgrade head

# 灌入商品到 PG / Qdrant（脚本见 scripts/）
/Users/yangqiduo/miniforge3/envs/omnicart/bin/python scripts/seed_postgresql.py
/Users/yangqiduo/miniforge3/envs/omnicart/bin/python scripts/seed_qdrant.py
```
> 注：`scripts/` 下的灌库脚本请在启用 `.env` 的前提下从项目根运行；重建前建议先备份，避免覆盖现有数据。
